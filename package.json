{
  "name": "@offlocal/mcp",
  "version": "0.5.0",
  "description": "Production context layer for AI coding agents. MCPs give agents tools; offlocal.ai gives agents production context (which project, which environment, which provider account, what's allowed, what needs approval, what happened last time).",
  "license": "Apache-2.0",
  "type": "module",
  "bin": {
    "offlocal-mcp": "dist/index.js",
    "offlocal": "dist/cli.js"
  },
  "exports": {
    "./policy": {
      "types": "./dist/policy-core.d.ts",
      "default": "./dist/policy-core.js"
    },
    "./package.json": "./package.json"
  },
  "files": [
    "dist",
    "docs",
    ".offlocal/config.example.yaml",
    ".env.example",
    "README.md",
    "LICENSE"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/adi4x4/offlocalai-mcp.git"
  },
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx src/index.ts",
    "cli": "tsx src/cli.ts",
    "start": "node dist/index.js",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "tsc -p tsconfig.json --noEmit",
    "verify": "npm run typecheck && npm run build && npm test && npm audit",
    "prepublishOnly": "npm run verify"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "fast-xml-parser": "^5.8.0",
    "yaml": "^2.5.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^3.2.6"
  },
  "engines": {
    "node": ">=18"
  },
  "overrides": {
    "vite": "6.4.2"
  }
}
